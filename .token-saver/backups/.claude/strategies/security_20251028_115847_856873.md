# Security

- Data should be robustly encrypted at rest and in transit
- There is never a time where the only validation is happening on the front end. All data from the front end should not be trusted.
- Authentication and Identity Management should assume the principle of least privilege and be heavily tested.
- We should have zero packages or dependencies that have security vulnerabilities.

